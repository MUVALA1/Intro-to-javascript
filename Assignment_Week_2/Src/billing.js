//This is the percentage that determines the transaction charge
const transactionCharge=2.5%;
var amountTendered=100000;
let amount="This is what the billed amount is(less transaction charges)";
console.log(amount);
amount=amountTendered-transactionCharge
console.log(amount);
